
#include "packagedb.h"

using namespace systmatic;

int main(int argc, char* argv[])
{
	// The database object
	packagedb db;
	// Create an empty db
	db.createdb();

	// That's it for now
	return 0;
}
